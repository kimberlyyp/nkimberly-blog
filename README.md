# nkimberly

This site was converted from WordPress and is hosted on GitHub Pages.

## Local Development

To run locally:

```bash
bundle install
bundle exec jekyll serve
```

Then visit http://localhost:4000
